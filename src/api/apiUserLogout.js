export default async function apiLogOutUser() {
  try {
    // console.log('logout fetch');
    // let response = await fetch(`/user:id`, {  //:id는 어떻게?
    //   method: 'GET',
    //   mode: 'cors',
    // });
    // response = response.json();
    // return response;
  } catch (err) {
    console.error(err, 'logout error');
  }
}
